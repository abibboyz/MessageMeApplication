
      <div class="ui inverted menu">
        <div class = "ui container">
          <a class="item">
            ChatRoom
          </a>
          <a class="item">
            Messages
          </a>
          <a class="item">
            Friends
          </a>
        </div>
        
        <div class="ui simple dropdown item">
          Account<i class="dropdown icon"></i>
          <div class="menu">
            <% if logged_in? %>
              <%= link_to "Logout", logout_path, data: { turbo_method: :delete, turbo_confirm: 'Are you sure?' }, class: "item" %>
            <% else %>
              <%= link_to "Log in", login_path, class: "item" %>
            <% end %>
        
      </div>
    </div>
    <% if !logged_in? %>
    <div class="item">
      <%= link_to "Log in", login_path, class: "ui primary button" %>
    </div>
  <% end %>
</div>